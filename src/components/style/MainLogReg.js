import styled from "styled-components";

export const MainLogReg = styled.main`
    margin-top: 30px;
    grid-column: 2;
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: 40px;
    justify-content: start;
    align-items: center;
`